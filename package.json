{
  "main": "src/index.ts",
  "name": "antiscambottool",
  "scripts": {
    "sync": "npx slash-up@latest sync",
    "dev": "wrangler dev",
    "types": "wrangler types src/types/wrangler.d.ts",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "slash-create": "^6.8.1",
    "wrangler": "^4.78.0"
  },
  "dependencies": {
    "just-is-empty": "^3.4.1",
    "parse-duration": "^2.1.5"
  },
  "overrides": {
    "slash-create": {
      "undici": "^7.24.0"
    }
  },
  "engines": {
    "node": "24.11.x"
  }
}
