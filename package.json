{
  "main": "src/index.ts",
  "scripts": {
    "sync": "slash-up sync",
    "types": "wrangler types src/types.d.ts",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "slash-create": "^6.7.0",
    "slash-up": "^1.4.2",
    "wrangler": "^4.50.0"
  }
}
