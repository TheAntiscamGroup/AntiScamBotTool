{
  "main": "src/index.ts",
  "name": "antiscambottool",
  "scripts": {
    "sync": "slash-up sync",
    "types": "wrangler types src/types.d.ts",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "slash-create": "^6.7.1",
    "slash-up": "^1.4.2",
    "wrangler": "^4.53.0"
  }
}
